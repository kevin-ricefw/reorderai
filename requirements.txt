# Config
python-dotenv>=1.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
pyyaml>=6.0.0

# Database — Wecomm Azure Postgres
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0

# API
fastapi>=0.128.0
uvicorn[standard]>=0.27.0

# Math / forecasting (Phase 1)
pandas>=2.0.0
numpy>=1.26.0
pyarrow>=14.0.0
# Phase 1 Smooth path uses bootstrap; LightGBM optional for later upgrade
lightgbm>=4.0.0

# Optional GPT justification
openai>=1.40.0

# Tests
pytest>=8.0.0
