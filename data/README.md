# Local data

Store POS exports here. Contents are gitignored.

Expected folders (create as needed):

- `sales/` — daily POS CSV exports
- `inventory/` — on-hand inventory CSV
- `vendors/` — vendor catalogs
- `delivery_timings/` — vendor order schedule
- `Past Invoices/` — invoice receive patterns
- `waste/` — waste uploads
- `cache/` — runtime cache only
